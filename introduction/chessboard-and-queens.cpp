#include <bits/stdc++.h>

#define _ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define ll long long

#define LIM 1000000007

using namespace std;

int main() {_
    vector<string> board(8);
    for (int i = 0; i < 8; i++)
        cin >> board[i];

    

    return 0;
}