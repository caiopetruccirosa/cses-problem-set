#include <bits/stdc++.h>

#define _ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define ll long long
#define pll pair<long long, long long>

using namespace std;

int main() {_
    int n;
    cin >> n;

    ll sum = 0;
    vector<ll> p(n);
    for (int i = 0; i < n; i++) {
        cin >> p[i];
        sum += p[i];
    }

    sort(p.begin(), p.end());

    ll cost = 0;
    for (int i = 0; i < n; i++) {
        cost += ()
    }

    return 0;
}